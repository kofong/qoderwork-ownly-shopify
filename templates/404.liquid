<section class="not-found section-padding" style="text-align: center; min-height: 60dvh; display: flex; align-items: center; justify-content: center;">
  <div class="section-container">
    <h1 style="font-size: 6rem; font-weight: 800; color: var(--color-accent); line-height: 1;">404</h1>
    <p style="font-size: 1.25rem; margin: 1rem 0 2rem; color: var(--color-text-secondary);">Page not found</p>
    <a href="{{ routes.root_url }}" class="btn btn--primary">Back to Home</a>
  </div>
</section>
